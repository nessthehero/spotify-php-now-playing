/*global describe, it, require*/

'use strict';

const util = require('brei-util');

describe('brei-project-scaffold -- Test (test/test.js)', function () {
	it('Empty test', function () {
		util.assert(true);
	});
});